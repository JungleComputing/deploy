<project name="deploy" default="build" basedir=".">
    <description>
	Build file for the deploy project
    </description>

    <property name="version" value="" />

    <property name="dist-name" value="${ant.project.name}${version}" />

    <!-- Import environment properties -->

    <property environment="env" />
    <property name="gat-location" location="${env.GAT_LOCATION}" />
    <property name="ipl-home" location="${env.IPL_HOME}" />

    <target name="build" description="Build with included JavaGAT and IPL" depends="clean,copy-included,compile,copy-server-libs" />

    <target name="build-external" description="Build with external JavaGAT and IPL" depends="clean,copy-external,compile,copy-server-libs" />


    <!-- copy libraries needed to start a ibis-server and/or hub -->
    <target name="copy-server-libs">
        <mkdir dir="lib-server" />

        <copy todir="lib-server">
		<fileset dir="lib/ipl" 
			includes="commons*,ibis*,ipl*,jstun*,log4j*,sbbi*,slf4j*,smartsockets*,trilead*" />
	</copy>

    </target>

    <!-- copy external dependancies to lib, including provided GAT -->
    <target name="copy-included">
        <mkdir dir="lib" />

        <copy todir="lib">
            <fileset dir="external"/>
        </copy>

    </target>

    <!-- copy javaGAT specified by $GAT_LOCATION and IPL specified by $IPL_HOME  -->
    <target name="copy-external">
        <mkdir dir="lib" />

        <copy todir="lib">
            <fileset dir="external" includes="*.jar" />
        </copy>

        <copy todir="lib/javagat">
		<fileset dir="${gat-location}/lib" />
        </copy>

        <copy todir="lib/ipl">
            <fileset dir="${ipl-home}/lib" />
        </copy>
    </target>

    <!-- Compile -->
    <target name="compile">
        <mkdir dir="tmp" />
        <mkdir dir="lib" />

        <path id="default.classpath">
            <fileset dir="lib">
                <include name="*.jar" />
            </fileset>
            <fileset dir="lib/javagat">
                <include name="*.jar" />
            </fileset>
            <fileset dir="lib/ipl">
                <include name="*.jar" />
            </fileset>
            <pathelement path="tmp" />
        </path>

        <!-- Compile the java code from ${src} into ${tmp} -->
        <javac srcdir="src" destdir="tmp" debug="on" source="1.5" target="1.5" classpathref="default.classpath">
            <compilerarg value="-Xlint:unchecked" />
        </javac>

        <!-- Create jar file -->
        <jar destfile="lib/${dist-name}.jar" basedir="tmp" includes="**/*.class">
        </jar>

        <!-- Generate javadoc -->
        <javadoc destdir="javadoc" access="public" classpathref="default.classpath" packagenames="*" sourcepath="src" windowtitle="Ibis ${ant.project.name} Documentation" doctitle="Ibis ${ant.project.name} Documentation" bottom="The Ibis project">
            <link href="http://java.sun.com/j2se/1.5.0/docs/api/" />
        </javadoc>
        
        <delete dir="tmp" />

    </target>

    <!-- Clean up everything -->
    <target name="clean" description="Clean up">
        <delete dir="tmp" />
        <delete dir="lib" />
        <delete dir="lib-server" />
        <delete dir="javadoc" />
    </target>

</project>
